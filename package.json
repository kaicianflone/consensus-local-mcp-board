{
  "name": "consensus-local-mcp-board",
  "private": true,
  "workspaces": [
    "server",
    "web",
    "shared"
  ],
  "scripts": {
    "dev": "VERBOSE=1 concurrently \"npm --workspace shared run build:watch\" \"npm --workspace server run dev\" \"npm --workspace web run dev\"",
    "mcp:dev": "VERBOSE=1 npm --workspace server run mcp:dev",
    "build": "npm --workspace shared run build && npm --workspace server run build && npm --workspace web run build"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "concurrently": "^9.0.1"
  },
  "dependencies": {
    "@chat-adapter/slack": "^4.15.0",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "chat": "^4.15.0",
    "geist": "^1.7.0"
  }
}

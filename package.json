{
  "name": "@consensus/tools-mcp",
  "version": "0.1.0",
  "description": "Consensus.tools MCP Server",
  "type": "module",
  "main": "dist/server.js",
  "bin": {
    "consensus-mcp": "bin/cli.js"
  },
  "scripts": {
    "build": "tsup src/index.ts src/server.ts --format esm --dts",
    "test": "vitest run",
    "test:watch": "vitest",
    "publish": "npm publish --access public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "nanoid": "^5.1.6",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
